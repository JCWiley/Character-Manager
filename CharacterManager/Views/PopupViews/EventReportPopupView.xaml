<UserControl x:Class="CharacterManager.Views.PopupViews.EventReportPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CharacterManager.Views.PopupViews"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="Auto" Width="Auto">
    <Grid>
        <DataGrid x:Name="EventDisplayDataGrid" 
                  HorizontalAlignment="Left" 
                  Height="373" 
                  VerticalAlignment="Top" 
                  Width="772" 
                  Margin="10,10,0,0" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="True"
                  CanUserReorderColumns="True"
                  ItemsSource = "{Binding Events}"/>
        <Button x:Name="Close_Button" Content="Close" HorizontalAlignment="Left" Margin="669,388,0,0" VerticalAlignment="Top" Width="113" Command="{Binding CommandClose}" />

    </Grid>
</UserControl>
