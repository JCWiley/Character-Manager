<UserControl x:Class="CharacterManager.Views.EntityListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CharacterManager.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Red">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TreeView x:Name="EnityTree"
                  ItemsSource="{Binding EntityTree}"
                  SelectedItemChanged="EnityTree_SelectedItemChanged"
                  Padding="0,1,1,1"
                  BorderThickness="1"
                  Grid.Row="0">
            
        </TreeView>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="FILTER_REGION"/>

    </Grid>
</UserControl>
